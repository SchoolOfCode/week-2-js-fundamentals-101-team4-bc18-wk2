//a JavaScript function `convertGBPtoEUR` that takes in a specified amount in £, 
//and converts it to € using a given exchange rate. 
//The function should return the converted amount.
function convertGBPtoEU (GBP){
    return GBP * 1.21;
}